import type {MetadataRoute} from "next";import {services,industries,caseStudies,posts} from "@/data/site";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const staticRoutes=["","about","services","industries","portfolio","pricing","process","why-us","resources","faq","contact","consultation","privacy","terms"];return [...staticRoutes.map(x=>({url:`${base}/${x}`,lastModified:new Date()})),...services.map(x=>({url:`${base}/services/${x.slug}`,lastModified:new Date()})),...industries.map(x=>({url:`${base}/industries/${x.slug}`,lastModified:new Date()})),...caseStudies.map(x=>({url:`${base}/portfolio/${x.slug}`,lastModified:new Date()})),...posts.map(x=>({url:`${base}/resources/${x.slug}`,lastModified:new Date()}))]}
